function recordsCard(){const all=records(),summary=validationSummary();return card('Local records',`<div class="kv"><b>Learning</b><span>${all.filter(r=>r.kind==='learning').length}</span><b>Work</b><span>${all.filter(r=>r.kind==='work').length}</span><b>Market</b><span>${all.filter(r=>r.kind==='market').length}</span><b>Governance</b><span>${all.filter(r=>r.kind==='governance').length}</span><b>Validations</b><span>${validations().length}</span><b>Peers</b><span>${peers().length}</span></div><small>${escapeHtml(summary.label)}</small><div class="row">${button('export:json','Export JSON')}${button('records:clear','Clear local data','class="danger"')}</div>`)}
function exportBackup(){const payload={schema:'civweave.chat.backup.v2',version:VERSION,exportedAt:now(),working:read(KEYS.working,{}),inbox:read(KEYS.inbox,[]),records:records(),validations:validations(),rewards:read(KEYS.rewards,{}),creator:creator(),peers:peers(),pairingInbox:pairingInbox(),settings:settings()};const url=URL.createObjectURL(new Blob([JSON.stringify(payload,null,2)],{type:'application/json'}));const a=document.createElement('a');a.href=url;a.download=`civweave-chat-${new Date().toISOString().slice(0,10)}.json`;a.click();setTimeout(()=>URL.revokeObjectURL(url),1000);}

function field(root,name){const el=root.querySelector(`[data-field="${name}"]`);return clean(el?.value,8000)}
function findRoot(target){return target.closest('.message-body')||target.parentElement}

async function openTool(id){const guide=id==='learn'?'moss':id==='work'?'kamiya':id==='market'?'rook':id==='govern'||id==='validate'?'merlin':'weaveling';let html='';switch(id){case'tools':html=toolMenu();break;case'wish':html=wishCard();break;case'learn':case'work':case'market':case'govern':html=recordTool(id);break;case'validate':html=validationTool();break;case'wallet':html=walletCard();break;case'nodes':await refreshPairingState();html=minimapCard();break;case'creator':html=creatorEditor(true);break;case'downloads':html=downloadsCard();break;case'records':html=recordsCard();break;case'settings':html=settingsCard();break;case'export':exportBackup();html=card('Export','<p>Portable backup created.</p>');break;case'rewards':html=rewardWallet();break;default:html=toolMenu();}addMessage({guide,html});}

function deterministicRoute(text){const t=text.toLowerCase();if(/node|peer|pair|nearby|gossip/.test(t))return'nodes';if(/verify|validate|validator|confidence threshold|payout gate/.test(t))return'validate';if(/learn|study|course|skill|module|school/.test(t))return'learn';if(/work|task|quest|build|submit|evidence/.test(t))return'work';if(/buy|sell|service|material|market|price|hire/.test(t))return'market';if(/vote|govern|proposal|consent|role|appeal/.test(t))return'govern';if(/wallet|credit|top.?up|payment|balance/.test(t))return'wallet';if(/download|model|local ai/.test(t))return'downloads';if(/setting|host|endpoint|privacy/.test(t))return'settings';if(/wish|intention|want|goal|become true/.test(t))return'wish';return null;}
const ROUTE_IDS=['wish','learn','work','market','govern','validate','wallet','nodes','creator','downloads','records','settings'];
function parseRoute(value){const match=String(value||'').toLowerCase().match(/\b(wish|learn|work|market|govern|validate|wallet|nodes|creator|downloads|records|settings|none)\b/);return match&&match[1]!=='none'?match[1]:null;}
async function chooseRoute(text){const s=settings();if((s.aiMode==='tiny'||s.aiMode==='auto')&&!tinyPipeline)await maybeLoadTiny();if((s.aiMode==='tiny'||s.aiMode==='auto')&&tinyPipeline){try{const out=await tinyGenerate(`Classify the user request. Output exactly one token from: ${ROUTE_IDS.join(', ')}, none. Request: ${text}`,null,{maxNewTokens:8});const hit=parseRoute(out);if(hit)return hit}catch{}}return deterministicRoute(text);}
async function hostedChat(text,guide){const s=settings();if(s.aiMode==='deterministic'||s.aiMode==='tiny'||s.aiMode==='local')return null;const host=s.serviceHost||location.origin,path=s.hostedPath||'/api/ai/chat';try{const body=await fetchJson(new URL(path,host),{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({message:text,guide,persona:{...GUIDES[guide],prompt:GUIDES[guide].prompt},context:{activeWeave:activeWeave(),validationSummary:validationSummary()}})});return clean(body?.text||body?.message||body?.output||body?.response,4000)||null}catch{return null}}
function routeMarkup(route){return route==='wish'?wishCard():route==='learn'||route==='work'||route==='market'||route==='govern'?recordTool(route):route==='validate'?validationTool():route==='wallet'?walletCard():route==='nodes'?minimapCard():route==='creator'?creatorEditor(true):route==='downloads'?downloadsCard():route==='records'?recordsCard():settingsCard()}
async function replyTo(text){setStatus('thinking');const guide=activeGuide,route=await chooseRoute(text);if(route){if(route==='nodes')await refreshPairingState();addMessage({guide:'weaveling',html:`<p>I routed that to the smallest useful chat tool.</p>${routeMarkup(route)}`});setStatus(tinyPipeline?'local router':'local');return}if(settings().aiMode==='auto'||settings().aiMode==='local'){const local=await localChat(text,guide);if(local){addMessage({guide,text:local});setStatus('local API');return}}if((settings().aiMode==='tiny'||settings().aiMode==='auto')&&tinyPipeline){try{const g=GUIDES[guide],out=await tinyGenerate(`${g.prompt}\nAnswer briefly and concretely as ${g.name}. User: ${text}`);if(out){addMessage({guide,text:out});setStatus('local AI');return}}catch{}}const hosted=await hostedChat(text,guide);if(hosted){addMessage({guide,text:hosted});setStatus('online');return}addMessage({guide,html:`<p>I can keep this conversational, but no configured model answered and the routing floor found no specific tool.</p><div class="row">${button('tool:tools','Show tools')}${button('tool:downloads','Install tiny router LM')}</div>`});setStatus(tinyPipeline?'local AI':'local');}
